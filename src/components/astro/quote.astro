---
const { text, cite } = Astro.props
---

<blockquote
  cite={cite}
  class="my-4 border-l-4 border-blue-500 bg-blue-50 px-6 py-4 text-lg italic text-gray-800 dark:bg-blue-900/30 dark:text-gray-100"
>
  {text}
  {
    cite && (
      <footer class="mt-2 text-right text-base not-italic text-gray-600 dark:text-gray-300">
        — <span class="italic">{cite}</span>
      </footer>
    )
  }
</blockquote>
